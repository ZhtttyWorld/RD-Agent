interface:
  display_name: "RD Agent"
  short_description: "Repo-specific patterns and workflows for RD-Agent"
  default_prompt: "Use the RD-Agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true